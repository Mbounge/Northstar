# Canvas V2 Phase 6B — grounded evidence experience

Phase 6B connects the neutral Phase 6A research adapter to Apps and References.
Users can browse account apps, open a captured flow, inspect its exact ordered
screens, insert one screenshot, or insert the complete flow.

Complete flows compile into ordinary Canvas V2 HTML/CSS source. The canonical
presentation is intentionally restrained: one `GROUNDED EVIDENCE` region, a
left app identity, and full-height uncropped screenshots in an evenly spaced
horizontal lane. Additional flows become aligned rows in the same region.

Every icon and screenshot retains its approved evidence id and exact source URL
in the committed revision manifest. Flow lanes, identity, sequence, and every
screen receive stable canvas node ids, so the model and direct manipulation
tools can select and transform them without replacing the canonical evidence.
